// AppRoutes.jsx - This file is kept for backward compatibility
// Routing is now handled directly in App.jsx via React Router
// Each role has its own Dashboard component with tab-based navigation

export default null;
